# Space-specific requirements (Week 5 deployment).
#
# Trimmed from the project's root requirements.txt down to what
# deployment/huggingface/space_app.py -> src/dashboard/app.py actually
# imports at runtime. Verified against the real import chain (dashboard ->
# simulator, tools.monitor_tool, pipeline.full_pipeline -> the 4 agents ->
# their tools -> crewai/chromadb/xgboost) on 2026-08-19 -- nothing in that
# chain imports streamlit, fastapi, uvicorn, or shap, so they're left out
# here to keep the free-tier CPU Space's build fast and its image small.
#
# If you add a feature to the deployed dashboard that needs one of those
# (or anything else), add it here too -- this file is intentionally NOT
# just a copy of the root requirements.txt, so it will drift if you forget.
# When in doubt, use the root requirements.txt instead; it's a superset.

crewai==1.15.5
crewai-tools==1.15.5
litellm==1.91.4
python-dotenv
pandas
xgboost
scikit-learn
chromadb
gradio>=5.0

# Required only because this Space is currently assigned ZeroGPU hardware
# (Hugging Face free-tier dynamic-GPU pool). This app does no local GPU
# work -- XGBoost inference + remote Groq API calls only -- but the
# platform requires at least one @spaces.GPU-decorated function to exist
# or it kills the container at startup with "No @spaces.GPU function
# detected during startup." See the no-op function in space_app.py.
spaces
